

@{
    ViewData["Title"] = "My Decks";
}

<link href="~/css/Decks/Index.css" type="text/css" rel="stylesheet" />

<h1>My Decks</h1>

<hr />


<div id="main-actions">
    <div class="deck-buttons">
        <a asp-action="CreateDeck">Create a Deck</a>
    </div>

    <div class="search-bar">
        <form asp-action="Search" method="get">
            <input name="query" placeholder="Search public cards by tag" required>
            <input type="submit" value="Search" />
        </form>
    </div>
</div>

<hr />

<div class="decks"> @*This is used for reference in regards to JavaScript*@
    @*foreach (var item in Model)
        {
            <a class="deck-link" asp-action="ViewDeck" asp-route-deckID="@item.Id">
                <div class="deck" id="@item.Id">
                    <h1>@item.Name</h1>
                    <p>@item.Description</p>
                </div>
            </a>
        }*@
</div>
<div class="preload">
    <img src="~/images/preloader.gif" class="preload" />
</div>

<script src="~/js/LazyLoad.js"></script>
<script src="~/js/Decks/MyDecks.js"></script>